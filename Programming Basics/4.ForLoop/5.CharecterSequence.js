function simbols(input) {
               //let text = input[0];
               //let letter = text[4];едно и също, взима символ от текста по индекс
               //let letter = text.charAt(4); едно и също

               //console.log(letter);
               let text = input[0];

               for (let i = 0; i < text.length; i++)
               console.log(text[i]);
}
simbols(["softuni"])
simbols(["ice cream"])
