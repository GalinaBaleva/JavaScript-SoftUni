function substring(word, startingIndex, count){
               // console.log(word.slice(startingIndex, count + startingIndex));

               console.log(word.substring(startingIndex, count + startingIndex));
}
substring("ASentance", 1, 8);