function reversedChars(char1, char2, char3) {
               console.log(`${char3} ${char2} ${char1}`);

               console.log(result);
}
reversedChars('A', 'B', 'C');
reversedChars('1', 'L', '&');
